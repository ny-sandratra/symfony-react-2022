{% extends 'base.html.twig' %}

{% block title %}Moto{% endblock %}

{% block body %}
    <h1>Moto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moto.id }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ moto.modele }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ moto.marque }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ moto.couleur }}</td>
            </tr>
            <tr>
                <th>NumeroSerie</th>
                <td>{{ moto.numeroSerie }}</td>
            </tr>
            <tr>
                <th>NumeroMatricule</th>
                <td>{{ moto.numeroMatricule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moto_index') }}">back to list</a>

    <a href="{{ path('moto_edit', {'id': moto.id}) }}">edit</a>

    {{ include('moto/_delete_form.html.twig') }}
{% endblock %}
