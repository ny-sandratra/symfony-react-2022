{% extends 'base.html.twig' %} {% block title %}UserLogin index{% endblock %} {% block body %}
<h1>UserLogin index</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Username</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for user_login in user_logins %}
        <tr>
            <td>{{ user_login.id }}</td>
            <td>{{ user_login.username }}</td>
            <td>
                <a href="{{ path('user_login_delete', {'id': user_login.id}) }}">delete</a>
                <a href="{{ path('user_login_edit', {'id': user_login.id}) }}">edit</a>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="3">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

<a href="{{ path('user_login_new') }}">Create new</a> {% endblock %}