{% extends 'base.html.twig' %}

{% block title %}UserLogin{% endblock %}

{% block body %}
    <h1>UserLogin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_login.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user_login.username }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_login_index') }}">back to list</a>

    <a href="{{ path('user_login_edit', {'id': user_login.id}) }}">edit</a>

    {{ include('user_login/_delete_form.html.twig') }}
{% endblock %}
