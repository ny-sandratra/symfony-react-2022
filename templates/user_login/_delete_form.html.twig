<form method="post" action="{{ path('user_login_delete', {'id': user_login.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_login.id) }}">
    <button class="btn">Delete</button>
</form>
