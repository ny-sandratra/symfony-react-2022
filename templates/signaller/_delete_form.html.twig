<form method="post" action="{{ path('signaller_delete', {'id': signaller.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signaller.id) }}">
    <button class="btn">Delete</button>
</form>
